---
description: Real runtime capabilities first; thread-local override tests (default / positive / negative) and nonsensical-input handling.
globs: Development/Tests/**/*RuntimeCapability*.swift, Development/Tests/**/*Capability*.swift, Framework/Sources/Core/Models/RuntimeCapabilityDetection.swift
alwaysApply: false
---

# Real capabilities and thread-local overrides

## Principle

1. **Test real capabilities** on the **actual** OS (simulator or device) for that target — do not assert “platform Y behavior” from an iOS host using overrides alone. Use the **tvOS / macOS / watchOS** test targets and simulators when you need that platform’s runtime.

2. When a capability **can** be off at runtime on that platform, test **enabled and disabled** (or OS-reported states) against real or thread-local simulation **where that simulation is meaningful** on the current host.

3. When a “disabled” request is **nonsensical** for the platform (e.g. “no touch” on iPhone), **production code** should **detect it, ignore it, and keep correct behavior** (see `RuntimeCapabilityDetection.supportsTouch` / `setTestTouchSupport`). **Tests** must still run the negative path and **assert** ignore semantics — do not skip. Optional **logging** (e.g. once per thread when the nonsensical override is set) helps diagnose misuse in dev and prod.

## Thread-local `setTest*` — three flows on every platform

For each capability that supports `RuntimeCapabilityDetection.setTest*(...)`, structure coverage as:

1. **Default** — `clearAllCapabilityOverrides()` (often via `DefaultRuntimeCapabilityIsolationTrait`); assert behavior matches intrinsic runtime / platform detection and that consumers (e.g. `getCardExpansionPlatformConfig()`) mirror `RuntimeCapabilityDetection`.

2. **Positive override** — set the override to `true` when the platform can honor it; assert the code path that should run **when that capability is on** on **this** platform.

3. **Negative override** — set the override to `false` and **always run this path** (do not skip on iOS/watchOS):
   - If the platform **cannot** semantically disable the capability (e.g. **iOS/watchOS touch** — a platform guarantee), assert the **negative value is ignored** and documented behavior holds (`supportsTouch` still true; see `setTestTouchSupport` doc on `RuntimeCapabilityDetection`).
   - If the platform **can** simulate off (e.g. **macOS touch** via thread-local override), assert **false is respected** and downstream behavior matches.

Do not use overrides only to “pin the matrix” for unrelated assertions; matrix tests use native detection. Overrides belong in tests whose purpose is default vs positive vs negative semantics.

## `CapabilityOverride` (UI / `*WithOverride` APIs)

Separate from thread-local `setTest*`. Document in the test which layer is under test. `supportsTouchWithOverride` may still honor `CapabilityOverride.touchSupport` when testing UI override layers.
