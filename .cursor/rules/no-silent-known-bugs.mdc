---
description: Observed defects (confirmed or strongly suspected) are in scope or a follow-up; never silently ignore them. Nits are not defects.
alwaysApply: true
---

# No Silent Known Bugs

**Precedence:** standing user instruction.

## Intent

Help write correct code. If while hunting, fixing, or reviewing you observe something that is **actually wrong**, do not walk past it — whether or not it matches the issue you were hired to fix.

## Rule

Any **confirmed or strongly suspected defect** you observe while working is either:

1. **In this issue** — expand scope explicitly (acceptance + a comment), **or**
2. **A follow-up GitHub issue** — created immediately. A one-line discovery comment with the new number is enough.

**Do not silently ignore it.** “Out of scope / don’t expand / leave it / unrelated, skip” without a follow-up issue number is forbidden.

**Do not build a Related graph.** Link issue bodies only when they share a defect class or an explicit scope split (do not absorb A into B). “Found on the same file” is not related.

Uncertain is not ignore: state confidence (“I think…”, “unverified”) and still put it on the thread or an issue.

Do not drive-by-fix unless it is explicitly in this issue’s scope.

## Defect vs nit

**Defect** — correctness, identity/lifecycle, security, broken contracts, missing tests for claimed behavior, adjacent logic bugs on the path you are in. Name the site (type/file/call) and what fails or can fail.

**Nit** — style, naming taste, speculative refactors, “I would have written it differently.” Nits are not this rule. Do not file them.

Similarity is irrelevant. Same class, similar, or unrelated-but-wrong all follow the fork above.

## Example (scope split, not a Related graph)

- #474 UUID catalog identity (in scope) + collapse keyed on ephemeral `section.id` (adjacent, same issue)
- #475 other `id = UUID()` catalog types (follow-up, not ignored)
